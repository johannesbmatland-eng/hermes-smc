market: "BTC/USD"
paper_trading:
  initial_capital: 100000
  currency: "USD"

timeframes:
  main: "5m"
  trend_1h: "1h"
  trend_15m: "15m"

trend_filter:
  enabled: true
  method: "ema_majority"   # EMA majority across 5m/15m/1h (structure is informational)
  ema_period: 50

fvq_detection:
  min_candles_since_fvg: 50
  fvg_buffer_pct: 0.001

entry:
  confirmation: "engulfing_or_ifvg"
  pullback_depth_pct: 0.5
  max_open_positions: 1
  cooldown_seconds: 300
  engulf_lookback: 2      # bearish FVG-touch → engulf on 1st or 2nd next candle

rsi:
  enabled: true
  period: 14
  long_max: 65            # no longs when RSI above this
  short_min: 35           # no shorts when RSI below this

# KZP [TFO] killzones (America/New_York)
sessions:
  timezone: "America/New_York"
  filter_entries: true    # only open trades inside enabled windows
  windows:
    - { name: "ASIA", start: "20:00", end: "00:00", enabled: true }
    - { name: "LNDN", start: "02:00", end: "05:00", enabled: true }
    - { name: "NYAM", start: "09:30", end: "11:00", enabled: true }
    - { name: "NYL",  start: "12:00", end: "13:00", enabled: false }
    - { name: "NYPM", start: "13:30", end: "16:00", enabled: true }
    - { name: "RTH",  start: "09:30", end: "16:00", enabled: false }

exits:
  structure_break: false
  mode: "be_trail"       # fixed_tp | be_trail | trail_only
  be_at_rr: 1.5          # BE after 1.5R (avoids BTC noise scratch at 1R)
  trail_after_be: true   # then trail favorable extreme
  trail_rr: 1.0          # trail distance = 1R behind peak/trough
  tp_rr: 3.0             # hard TP at 3R (~1.5% account at 0.5% risk)
  be_buffer_pct: 0.0001  # tiny BE buffer past entry

risk:
  risk_pct_per_trade: 0.5
  rr_target: 2.0          # used for BE trigger alignment / legacy fixed TP
  rr_alternative: 3.0
  # SL just beyond FVG zone edge (gap), NOT under candle-1's low
  sl_buffer_pct: 0.0003
